services:
  portfolio:
    container_name: portfolio
    build:
      context: ./portfolio
      dockerfile: prod.Dockerfile
    restart: always
    ports:
      - 3000:3000
    networks:
      - my_network

networks:
  my_network:
    external: true